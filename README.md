## Different Environemnts Used for This Project:

1) SINGLE MACHINE, NO DOCKER (gRPC)
2) SINGLE MACHINE, NO DOCKER (REST)
3) SINGLE MACHINE WITH DOCKER (gRPC)
4) SINGLE MACHINE WITH DOCKER (REST)
5) MULTI-MACHINE (2 DEVICES) (gRPC) (WORKER USES WINDOWS, MASTER USES WSL2)
6) MULTI-MACHINE (2 DEVICES) (REST) (WORKER USES WINDOWS, MASTER USES WSL2)
7) SINGLE MACHINE, NO DOCKER, NO COMMUNICATION PROTOCOLS (MULTITHREADING, WSL2)
8) SINGLE MACHINE, NO DOCKER, NO COMMUNICATION PROTOCOLS (MULTIPROCESSING, WSL2)

